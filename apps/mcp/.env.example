# Supabase (service role: the MCP server runs locally, for the single owner account)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# The auth.users id the server acts as
HABITRON_USER_ID=

# IANA timezone used for "today" and "now" (defaults to the process timezone)
HABITRON_TIMEZONE=
